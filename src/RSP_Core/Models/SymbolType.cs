namespace RSP_Core.Models
{
    /// <summary>
    /// Three symbols in a rock-paper-scissors cycle
    /// Square > Triangle, Triangle > Circle, Circle > Square
    /// </summary>
    public enum SymbolType
    {
        Square,
        Triangle,
        Circle
    }
}
